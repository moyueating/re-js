# DFS
- 递归
- 栈
```js
    while(stack.length){
        let pop = stack.pop();
        ....
    }
```

# BFS
- 队列
```js
    while(queue.length){
        let size = queue.length;
        let first = queue.shift();
        for(let i = 0; i < size; i++){
            queue.push(...);
            ....
        }
    }
```